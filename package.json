{
  "name": "gift-platform",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && node app.js\"",
    "start": "node server/app.js",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "render-build": "npm run install-client && npm run install-server && npm run build"
  },
  "dependencies": {
    "react-router-dom": "^7.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
